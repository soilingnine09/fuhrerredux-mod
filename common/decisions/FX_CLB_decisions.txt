foreign_politics = {
    CLB_whitepeace_nid = {
        icon = GFX_decision_generic_civil_support
        cost = 20
        
        allowed = { original_tag = CLB }
        visible = { has_war_with = NID }
        available = {
            NID = { surrender_progress > 0.1 }
            CLB = { surrender_progress > 0.1 }
        }
        complete_effect = {
            NID = { country_event = { id = shoshone.2 hours = 8 } }
        }
    }
}